@using SimpleStore.Web.Areas.Administration.ViewModels
@model IEnumerable<OrderDetailViewModel>

<div class="card">
    <div class="card-body">
        <div class="list-group list-group-flush">
            @foreach (var detail in Model)
            {
                <partial name="_Detail" model="detail" />
            }
        </div>
    </div>
</div>